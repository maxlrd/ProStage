{% extends 'base.html.twig' %}

{% block contenu %}
<ul class="list-group">
	{% for formationCourante in formations %}
	<li class="list-group-item center">
		<a class="nav-link disabled" href="{{path('prostage_stageByFormation', {id:formationCourante.id})}}">
			{{formationCourante.nom}}
		</a>		
	</li>
	{% endfor %}
</ul>
{% endblock %}
